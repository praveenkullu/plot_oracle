Start plot-oracle-3000 (Express API) and view logs.
```bash
cd "/home/praveen/LTU/MCS5993_DCB/dev/plot_oracle" && ~/.local/bin/pm2 start ecosystem.config.cjs --only plot-oracle-3000 && ~/.local/bin/pm2 logs plot-oracle-3000
```
